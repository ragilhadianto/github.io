/* ------------------------------------------------------------------------------
@name: ScaneGlobal
@description: ScaneGlobal
--------------------------------------------------------------------------------- */

// - ScaneGlobal
const ScaneGlobal = (() => {

  const handleScaneGlobal = () => {
    
  };



  // - init
  const init = () => {
    handleScaneGlobal();
  };

  // return
  return {
    init,
  };
})();

export default ScaneGlobal;

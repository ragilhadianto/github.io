/* ------------------------------------------------------------------------------
@name: ScrollAnimate
@description: ScrollAnimate
--------------------------------------------------------------------------------- */

// - ScrollAnimate
const ScrollAnimate = (() => {
  // - handleScrollAnimate
  const handleScrollAnimate = () => {};

  // - init
  const init = () => {
    // handleScrollAnimate();
  };

  // return
  return {
    init,
  };
})();

export default ScrollAnimate;
